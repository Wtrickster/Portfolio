// Config starter code
import { createChatBotMessage } from "react-chatbot-kit";

const config = {
  bot: "Question bot",
  initialMessages: [createChatBotMessage(`Hello, please ask any question you might have`)]
}

export default config